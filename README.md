# Stage 9: CNN Autoencoder Reconstruction

This stage trains a neural network to find patterns in the hybrid-encrypted images and attempt visual reconstruction.

## Execution

1. Ensure Stage 8 `.npy` files exist in `datasets/ml_dataset/`.
2. Run:

```bash
export PYTHONPATH=$PYTHONPATH:.
python main.py
```
