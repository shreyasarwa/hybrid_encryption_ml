# Machine Learning Analysis of Hybrid Image Encryption

Professional modular framework for analyzing hybrid encryption (Permutation + XOR) systems using ML.

## Setup Instructions

1. **Clone & Initialize**

   ```bash
   git clone <repo_url>
   python init_dirs.py

   ```

# Stage 1: Dataset Collection & Preparation

This module downloads the CIFAR-10 dataset and converts the NumPy tensors into physical image files for the encryption pipeline.

## Terminal Commands

### 1. Environment Setup (If not done in Stage 0)

```bash
python3 -m venv venv
source venv/bin/activate  # Windows: .\venv\Scripts\Activate.ps1
pip install -r requirements.txt
```
